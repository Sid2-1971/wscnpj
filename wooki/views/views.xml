<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="wooki.list">
      <field name="name">wooki list</field>
      <field name="model">wooki.wooki</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="wooki.action_window">
      <field name="name">wooki window</field>
      <field name="res_model">wooki.wooki</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="wooki.action_server">
      <field name="name">wooki server</field>
      <field name="model_id" ref="model_wooki_wooki"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="wooki" id="wooki.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="wooki.menu_1" parent="wooki.menu_root"/>
    <menuitem name="Menu 2" id="wooki.menu_2" parent="wooki.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="wooki.menu_1_list" parent="wooki.menu_1"
              action="wooki.action_window"/>
    <menuitem name="Server to list" id="wooki" parent="wooki.menu_2"
              action="wooki.action_server"/>
    -->
  </data>
</odoo>