<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="infoplex.list">
      <field name="name">infoplex list</field>
      <field name="model">infoplex.infoplex</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="infoplex.action_window">
      <field name="name">infoplex window</field>
      <field name="res_model">infoplex.infoplex</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="infoplex.action_server">
      <field name="name">infoplex server</field>
      <field name="model_id" ref="model_infoplex_infoplex"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="infoplex" id="infoplex.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="infoplex.menu_1" parent="infoplex.menu_root"/>
    <menuitem name="Menu 2" id="infoplex.menu_2" parent="infoplex.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="infoplex.menu_1_list" parent="infoplex.menu_1"
              action="infoplex.action_window"/>
    <menuitem name="Server to list" id="infoplex" parent="infoplex.menu_2"
              action="infoplex.action_server"/>
    -->
  </data>
</odoo>